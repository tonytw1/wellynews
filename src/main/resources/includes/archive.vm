#if ($archive_links)
<h2>Archive</h2>

#if ($site_count && $newsitem_count && $feed_count)
        <p>Currently tracking $site_count websites, $feed_count feeds and $newsitem_count newsitems.</p>
#end

<ul>
    #parse('includes/archiveLinkList.vm')
    <li class="more"><a href="$siteInformation.url/archive">More...</a></li>
</ul>
#end
