#parse("includes/header.vm")

<div class="page">
#parse("includes/topbar.vm")

<div class="left">
#parse("includes/tag_tree.vm")
</div>

<div class="main">
<h2>$heading</h2>

<p>The news watchlist looks for changes on pages known to contain news items.</p>

<p>Automatically finding new content on sites which do not provide news feeds is not an exact science.
The watchlist tries to assist by highlighting pages which have changed for manual checking.</p>

<p>The Watchlist is available as an RSS feed:</p>
<ul>
	<li>#parse("includes/rss_icon.vm") <code>$rss_url</code></li>
</ul>

#parse("includes/main_content.vm")

#parse("includes/page_links.vm")
</div>

<div class="right">

<div class="box">
#parse("includes/latest_newsitems.vm")
<br clear="all"/>

</div>

</div>

</div>

#parse("includes/footer.vm")