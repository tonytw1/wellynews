#if ($geocode)
	<p>
	#if($urlBuilder.getLocationUrlFor($geocode)) 
		<a href="$urlBuilder.getLocationUrlFor($geocode)">#parse("includes/geotag_icon.vm")$geocode.address</a>
	#else
		#parse("includes/geotag_icon.vm")
		$geocode.address
	#end
	#if ($geocode.osmId)
		(<a href="$urlBuilder.getOsmWebsiteUrl($geocode.osmId)">OpenStreetMap</a>
		    $geocode.osmId
		)
	#end
	</p>
#end